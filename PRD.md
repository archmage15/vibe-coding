# PRD：照片「压到能传上去」· 纯浏览器图片压缩器

| 项目 | 内容 |
|---|---|
| 文档类型 | **产品需求文档（PRD）** —— Vibe Coding 五步工作流第 3 步产出 |
| 项目名称 | 照片压到指定大小以内（纯浏览器图片压缩器） |
| 日期 | 2026-09-19（Day 4 · 第 1 周） |
| 阶段 | 需求定义（定「做成什么样」） |
| 上游 | research.md（Day 3 需求研究，已入库） |
| 下游 | Day 5 技术选型 → 写代码 |
| 状态 | 已完成初稿，待 AI 自检与用户核对 |

> 本文档只定义**做什么、做成什么样算合格**，不规定**用什么技术做**（技术选型是 Day 5 的事，今天不碰）。

---

## 0. 项目一句话

一个打开就能用的网页工具，**把手机里那张「传不上去」的照片，按报名系统的要求（1M / 2M / 5M / 200KB / 50KB）一键压到大小之内**——全程在用户自己的浏览器里完成，照片不上传任何服务器。

**差异化一句话：不让你算数字。** 同类工具要么让你自己猜质量、要么丢给你一个滑块自己试；我们的做法是给档位按钮，压不到就用人话把原因说清楚。

---

## 1. 背景与要解决的问题

国内高校、政务、竞赛的报名 / 材料系统，对上传照片有严格的体积上限（常见 1MB / 2MB / 5MB，证件照甚至 200KB / 50KB）。真实痛点来自 research.md 第 1.5 节：

1. **「图片过大，请上传 2M 以内的照片」** —— 1200 万像素手机拍一张约 5~6MB，单张就超；超一点点整份被驳回，得从头再来。
2. **「压完还是超」** —— 多数工具不给目标大小，只能反复试、反复上传。
3. **「我这张照片上传后去了哪？」** —— 证件照、含人脸照、合同等敏感照片，用户普遍不安，尤其是要上传到第三方服务器时。
4. **「我只想要一张，你却给我一个 ZIP」** —— 打包下载对小批量是负担。
5. **「参数太多，看不懂就关了」** —— 精细控参工具对小白极不友好。

我们的机会：existing 同类「压到指定大小」工具功能不新（research 第 5 节已确认），但**没人把这事做成「中文档位 + 说人话提示 + 可验证的不上传」**。

---

## 2. 目标用户与场景

| 项 | 说明 |
|---|---|
| 目标用户 | 不会用电脑 / 不懂图像参数的同学；临时要交材料的人 |
| 核心场景 | 报名截止前，手里有张被系统拒掉的照片，想在 1 分钟内拿到能传的文件 |
| 使用环境 | 任意带浏览器的设备（手机 / 电脑均可），无需安装、无需联网（处理在本地） |
| 成功标准（用户视角） | 从「被拒绝」到「拿到能上传的文件」全程 ≤ 1 分钟，且照片没出过自己的设备 |

**非用户**（本期明确不服务）：专业修图师、需要逐张精细调参的人、需要批量处理上百张的人。

---

## 3. 核心功能清单（MVP 范围）

本期 MVP 只做下面 7 项，编号为 M1–M7（与 research.md 第 3 节一致）。

| 编号 | 功能 | 说明（用户能感知到的行为） |
|---|---|---|
| M1 | 拖入 / 选择图片 | 支持多张一起拖入或选择 |
| M2 | 选目标档位 | 1M / 2M / 5M / 200KB / 50KB 按钮，也可手填数字；默认给「常见报名上限」预设 |
| M3 | 自动压到目标以内 | 先降质量试，不够再降分辨率，尽最大努力卡进目标 |
| M4 | 前后对比 | 每张显示「原来多大 → 现在多大」，以及是否达标 |
| M5 | 单张下载 | 不要 ZIP 也能单独下一张；也能一次全下 |
| M6 | 说人话的失败提示 | 压不到目标时明确说明原因，并给可行建议（如「这张细节太多，建议先裁掉无关部分」） |
| M7 | 全过程本地处理 | 无上传、无账号、关掉页面即清空 |

---

## 4. 验收标准（做成什么样算合格）

> 这是本期交付的「合格线」。每一条都应是**可观察、可验证**的，AI 自检和人工核对都按它来。

**整体**
- AC-0：工具为纯前端网页，打开即用，**全程无任何网络上传请求**（可用断网 / F12 网络面板验证）。
- AC-1：界面为中文，无需注册登录即可使用。

**M1 拖入 / 选择**
- AC-M1：用户可一次拖入或选择 ≥ 1 张图片；选中后页面立即显示缩略图与原始体积。

**M2 选目标档位**
- AC-M2：页面提供 1M / 2M / 5M / 200KB / 50KB 档位按钮，且有一个默认选中项；同时支持手填任意正整数 KB/MB。

**M3 自动压到目标以内**
- AC-M3a：给定一张原始体积 > 目标的图片，工具产出文件体积 **≤ 目标值**（达标情形）。
- AC-M3b：若质量降到下限且分辨率已降仍无法达到目标，**不假装成功**，而是如实输出「实际体积 X，未达目标」并进入 M6 提示（这是允许的合格行为，不算失败）。

**M4 前后对比**
- AC-M4：每张处理完显示「原始体积 → 压缩后体积」与「达标 / 未达标」状态标识。

**M5 单张下载**
- AC-M5a：每张图可单独下载，下载的是该张压缩结果，不是打包 ZIP。
- AC-M5b：提供「全部下载」入口，可一次取得所有结果。

**M6 说人话的失败提示**
- AC-M6：当某张图未达标时，页面用中文说明原因（如体积仍超、细节过多等）并给一条可行建议。

**M7 本地处理 / 隐私**
- AC-M7：关闭页面后，本次处理的图片不残留于任何服务器；页面明确告知「照片只在你设备上处理」。

**范围外确认**
- AC-OUT：PRD 第 5 节列出的 8 项「不做」功能，本期交付物中均不出现。

---

## 5. 范围外 / 砍掉的功能及原因

> 本节是 Day 4 的重点：**明确不做什么，以及为什么。** 每一条砍除都服务于「纯本地 + 傻瓜 + 中文档位」这一唯一差异化，不是偷懒。

**A 类 · 与「纯本地、无服务器」定位冲突（做了就违背隐私初心）**
1. **不做注册登录** —— 纯前端没有服务器，本就不需要账号；一旦有账号就得有后端存储，照片就可能出设备，直接毁掉隐私卖点。
2. **不做云端存储 / 跨设备同步** —— 「关掉网页即清空」本身就是卖点，加云端反而增加泄露面与成本。
3. **不做 AI 去背景 / 图片放大 / 抠图** —— 这些依赖服务器算力，与「纯本地运行」根本冲突。

**B 类 · 与「傻瓜批量」用户定位冲突（做了就去抢 Squoosh 的地盘，丢了自己的用户）**
4. **不做高级参数**（编码器选择、色彩空间、色度采样、逐张微调）—— 那是给懂行的人用的；我们的用户是「三十张照片要交材料」的小白，给参数他就会关页面。
5. **不承诺支撑超大批量** —— 浏览器内存有限，本期只保 20 张以内稳定可用，不去吹「无限张」。

**C 类 · 范围控制（摊子太大 / 能吹的牛才吹）**
6. **不做视频、不做 PDF** —— 只做图片，先把一件事做透。
7. **不做多语言** —— 只做中文界面，降低本期复杂度。
8. **不承诺「一定能压到目标以内」** —— 质量降到下限、尺寸也降了仍达不到时，如实告知结果。这是**唯一不能吹的牛**：宁可不达标，也不假装成功。

---

## 6. 风险与待核实项

以下内容写进开发前**必须核实**，不能在文档里当既定事实引用（来自 research.md 第 6 节）：

1. 纯前端能否稳定把任意 5MB 照片压进 200KB / 50KB，需在 Day 5 后用小实验亲自验证，不能只信资料。
2. 各竞品上传留存时长、价格数字、免费次数限制等第三方说法，均待核实，PRD 中不引用为事实。
3. 中文报名系统常见档位（1M/2M/5M/200KB/50KB）是否覆盖绝大多数场景，需抽样核实；若发现高频缺失档位（如 10MB），应在 M2 中补充。
4. HEIC（iPhone 默认格式）多数系统不认，是否在本期支持需 Day 5 技术选型时定。

---

*本文档由 Day 4 工作产出，作为 Day 5 技术选型与后续编码的需求依据。*
