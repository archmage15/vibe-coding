# 技术设计：照片「压到能传上去」· 纯浏览器图片压缩器

| 项目 | 内容 |
|---|---|
| 文档类型 | **技术设计文档（TECH_DESIGN）** —— Vibe Coding 五步工作流第 4 步产出 |
| 项目名称 | 照片压到指定大小以内（纯浏览器图片压缩器） |
| 日期 | 2026-09-20（Day 5 · 第 1 周） |
| 阶段 | 技术选型（定「用什么做」） |
| 上游 | PRD.md（Day 4 需求定义，已入库） |
| 下游 | Day 6 起写代码 |
| 状态 | 已完成初稿，待用户核对 |

> 本文档只回答**用什么技术做、数据怎么流**，不写具体代码。
> 需求依据一律以 PRD.md 为准；PRD 里标注「待核实」的项，本文档不当作事实引用。

---

## 0. 先看这一句：数据从哪来、到哪去

> **照片从用户的设备来，在用户的浏览器里被压缩，直接回到用户的下载文件夹——全程不出这台设备。**
>
> 服务器在整件事里只干一件活：用户第一次打开网址时，把网页文件送给他。送完就没它事了。

这句话是本文档的地基。下面所有章节都在解释它为什么成立、以及怎么做到。

---

## 1. 技术路线与选型理由

### 1.1 技术路线一句话（结论）

> **纯前端、无后端、无数据库。用浏览器原生 ESM 直接加载 jSquash（WebAssembly 编解码器）完成压缩，页面文件托管在 GitHub Pages。**
> **零构建工具、零服务器、零费用、零用户数据。**

### 1.2 三套候选方案

比较维度只保留了真正有分歧的两项——**压缩引擎**和**部署方式**；「后端」和「数据库」两栏在三套方案里都是「没有」，这是 PRD 第 5 节 A 类砍除项决定的，不是遗漏。

| | **路线甲 · 素装** | **路线乙 · 带外挂（已选）** | **路线丙 · 全工程化** |
|---|---|---|---|
| 要装什么 | 什么都不用装 | 什么都不用装 | 要装 Node、跑 npm 命令 |
| 压缩引擎 | 浏览器自带 Canvas | jSquash（WebAssembly，MozJPEG / OxiPNG） | jSquash（WebAssembly） |
| 压缩能力 | 够用；极端目标吃力 | 强 | 强 |
| 首次打开 | 快 | 要额外加载 wasm 文件，稍慢 | 要等构建产物 |
| 部署 | GitHub Pages | GitHub Pages | Vercel / Cloudflare Pages |
| 排错难度 | 最低 | 中 | 高（构建失败、版本冲突） |
| 28 天做得完吗 | 能 | 能 | 存疑 |
| 费用 | 0 元 | 0 元 | 0 元（免费档额度以官网为准） |

三套的**费用都是零**。区别不在钱，在于**出错时要花多少精力才能自己搞明白**。

### 1.3 为什么选路线乙

**① 压缩能力是这款产品的命门，不能省。**
PRD 第 6 节第 1 条承认我们是「先相信浏览器能压，再验证」。路线甲（浏览器自带编码器）是黑盒：只能给一个质量值，不能控制算法，同画质下输出体积偏大。而我们的用户要的是「卡进 200KB / 50KB」这种硬指标——**越压不下去，M6「说人话的失败提示」就要说得越多**，产品体验直接退化。路线乙的 MozJPEG 带 trellis 量化（一种让同画质文件更小的编码技术），是专门为「体积吃紧」这个场景准备的。

**② 我们有办法在 WASM 出问题时活下来。**
路线乙最大的风险是「wasm 加载失败」（CDN 抽风、首次打开断网）。但 jSquash 的接口设计和浏览器原生编码器可以并存——**同一份像素数据，WASM 编码失败就换 Canvas 编码重试**。也就是说路线乙自带一条退回路线甲的活路，这个风险可控。见第 6 节「错误处理」和第 8 节「迁移」。

**③ 绕开了一个已知大坑。**
jSquash 官方文档记录：在 Vite / Nuxt 这类构建环境里，wasm 会报 `Failed to construct 'URL': Invalid URL` 或 `Aborted(both async and sync fetching of the wasm failed)`，需要在构建配置里做特殊处理。**我们不用构建工具，本来就绕开了这个坑**——这是「不装工具」带来的直接好处，不是省事省出来的。

### 1.4 代价与排除项（必须知道）

| 项 | 说明 |
|---|---|
| **代价 1** | 首次打开要多下载一个 wasm 文件（第三方记录 MozJPEG 约 246KB、OxiPNG 约 160KB，**待核实**）。用户第一次打开会慢一点。 |
| **代价 2** | 依赖第三方 CDN 提供代码，国内可访问性**未实测**。缓解方案见第 8 节「迁移 1」。 |
| **代价 3** | 首次打开需要联网（之后浏览器会缓存）。但这**不影响** AC-0「全程无上传请求」——下载页面代码是正常访问，不是上传照片。 |
| **排除 · 路线丙** | 构建工具对零基础是黑盒：出错信息（构建失败、依赖冲突）无法自己排查，而它换来的能力（组件化、包管理）我们这个单页小工具用不上。 |
| **排除 · 后端 + 数据库** | PRD 第 5 节 A 类第 1–3 条已明确砍除：注册登录、云端存储、服务器算力功能全部不做。**一旦引入后端，AC-M7 的隐私承诺就没了**，等于把唯一的产品卖点拆掉。 |
| **本期不做 · HEIC** | iPhone 默认格式。jSquash 没有 HEIC 解码器，支持它要另外引入一个 wasm 解码器，体积和排错成本都翻倍，且 PRD 的 MVP 七项里没有这一项（PRD 第 6 节第 4 条列为待定）。**本期遇到 HEIC 文件时明确提示用户「请先转成 JPG」，不假装支持。** |

---

## 2. 项目结构

纯静态站点，无构建步骤，所有文件直接来自仓库。

```
vibe coding/                      ← 仓库根目录（GitHub Pages 从这里发布）
├── index.html                    ← 唯一页面。用户看到的全部界面
├── app.js                        ← 主逻辑：文件流、档位、调度压缩、渲染结果
├── compress.js                   ← 压缩引擎：封装 jSquash 与 Canvas 两条路径
├── style.css                     ← 样式
├── .nojekyll                     ← 让 GitHub Pages 原样发布所有文件（见第 8 节）
│
├── AGENTS.md                     ← Day 1 协作规则（非产品文件）
├── research.md                   ← Day 3 需求研究（非产品文件）
├── PRD.md                        ← Day 4 产品需求（非产品文件）
└── TECH_DESIGN.md                ← 本文档（非产品文件）
```

**说明三条：**

1. **`index.html` 是改造现有文件，不是新建。** 它现在的内容是 Day 2 的 Git 练习占位页，最终会被产品界面替换掉。所以仓库里始终只有一个页面入口。
2. **只拆两个 JS 文件，不再细拆。** `app.js` 管界面和数据，`compress.js` 管压缩算法。这样分工的依据是：**压缩引擎将来可能整个换掉（甲↔乙），换的时候只动一个文件**。这也是不给零基础项目上构建工具的前提——没有模块打包，靠浏览器原生 ESM 就够。
3. **文档文件也会被公开发布。** GitHub Pages 会把仓库里的文件都发布到网上，`.md` 文档同样可被访问。仓库本来就是公开的，这不构成新增暴露，但需要知情。

---

## 3. 数据模型及字段

**这里没有数据库。** 本节定义的是**浏览器内存里流转的数据对象**——它们只活在页面打开期间，关掉页面就消失（对应 AC-M7）。

### 3.1 一张图的任务记录（ImageJob）

每拖进一张图，就生成一条：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 本条记录的唯一标识，用「文件名 + 时间戳」拼成 |
| `fileName` | string | 原始文件名（下载时用，重名时加序号） |
| `fileSize` | number | 原始体积，单位字节 |
| `mimeType` | string | 原始格式，如 `image/jpeg` |
| `status` | 枚举 | `待处理` / `处理中` / `已达标` / `未达标` / `读取失败` |
| `previewUrl` | string | 原图预览用的临时地址（`URL.createObjectURL` 产生，**用完必须 revoke，否则内存泄漏**） |
| `resultBlob` | Blob 或 null | 压缩后的字节，处理完成才有 |
| `resultSize` | number 或 null | 压缩后体积，单位字节 |
| `attempts` | number | 实际试了几轮 |
| `usedQuality` | number 或 null | 最终采用的质量值（0–1） |
| `usedScale` | number 或 null | 最终采用的缩放比例（1 = 没缩） |
| `message` | string | 给用户看的那句人话（M6 用） |

### 3.2 当前设置（Settings）

| 字段 | 类型 | 说明 |
|---|---|---|
| `preset` | string | 档位名：`50KB` / `200KB` / `1M` / `2M` / `5M` / `自定义` |
| `targetBytes` | number | 目标体积，单位字节 |
| `isCustom` | boolean | 是否用户手填 |

### 3.3 档位预设表（写代码时直接照抄）

| 档位 | 目标字节数 | 对应场景（来自 research.md 第 1.5 节） |
|---|---|---|
| 50KB | 51200 | 证件照 |
| 200KB | 204800 | 证件照 / 严格系统 |
| 1M | 1048576 | 高校材料常见上限 |
| 2M | 2097152 | 政务 / 竞赛报名常见上限 |
| 5M | 5242880 | 高校信息登记上限 |

**⚠️ 一个必须注意的坑**：这里的 KB / MB 按 **1024** 换算（1M = 1048576 字节），不按 1000。因为报名系统判超限时通常更严格，**保守取值能减少「我这边显示达标、传上去却被拒」的情况**。界面上要一句话说明这一点，让用户知道我们算的是严的那一版。

---

## 4. API 列表

**本项目不调用任何后端 API。** 下面两份清单是「写代码时要用的全部接口」。

### 4.1 浏览器原生 API

| API | 用途 | 对应功能 |
|---|---|---|
| `File` / `FileList` | 拿到用户选择的文件 | M1 |
| 拖放事件（`dragover` / `drop`） | 支持拖入 | M1 |
| `createImageBitmap(blob)` | 把图片解成可绘制的位图，速度快、能正确处理照片方向 | 解码 |
| `OffscreenCanvas` / `HTMLCanvasElement` | 承载像素、执行缩放 | 缩放 |
| `ImageData` | 像素数据的容器，编解码之间的中转格式 | 编解码中转 |
| `ArrayBuffer` / `Blob` | 字节容器 | 全程 |
| `URL.createObjectURL` / `revokeObjectURL` | 生成预览地址、生成下载地址 | M4 / M5 |
| `<a download>` | 触发浏览器下载 | M5 |
| `navigator.deviceMemory`（可选） | 粗略判断设备能否扛住批量 | 批量保护 |

### 4.2 jSquash（WebAssembly 编解码器）

通过浏览器原生 ESM 从 CDN 直接引入，**不需要 npm 安装**：

```js
import { decode, encode } from 'https://esm.sh/@jsquash/jpeg';
import { optimise }  from 'https://esm.sh/@jsquash/oxipng';
import resize        from 'https://esm.sh/@jsquash/resize';
```

| 接口 | 作用 | 说明 |
|---|---|---|
| `@jsquash/jpeg` 的 `decode` | JPEG 解码为像素 | 可选 `preserveOrientation` 保持照片方向 |
| `@jsquash/jpeg` 的 `encode` | 像素编码为 JPEG | 关键参数 `quality`（0–100）；这就是「降质量」的实现手段 |
| `@jsquash/oxipng` 的 `optimise` | PNG 无损优化 | PNG 路线的第二道处理 |
| `@jsquash/resize` | 高质量缩放 | 「降分辨率」的实现手段 |
| `@jsquash/png` / `@jsquash/webp` | PNG / WebP 编解码 | 备用，按需引入 |

**三条使用限制（来自官方文档，写代码时必须遵守）：**

1. **全部是 ESM 模块**，页面里必须写 `<script type="module">`。
2. **CSP 必须允许 wasm 执行**（`script-src 'wasm-unsafe-eval'`）。GitHub Pages 默认不加严格 CSP，所以不冲突。
3. 包本身**不含 wasm 二进制**，wasm 由 CDN 或本地目录提供。走 CDN 时 CDN 会自动处理；改成自托管时要手动放置（见第 8 节）。

### 4.3 明确不存在的接口

写代码时不要去找这些东西——**它们在这个项目里不存在**：

- ❌ 上传接口（`POST /upload` 之类）——没有服务器
- ❌ 用户登录 / 注册接口——没有账号系统
- ❌ 数据库读写接口——没有数据库
- ❌ 任何把照片发出去的请求——**有的话就是做错了**

---

## 5. 数据流

### 5.1 主图：照片从哪来、到哪去

```text
【照片的数据流 —— 全程在一台设备内完成】

  用户拖入 / 选择照片
  File 对象（此刻字节还在硬盘上，没进内存）
        │
        ▼
  ── 第一步：读进内存 ──
  原始字节 ArrayBuffer
        │
        ▼
  解码 decode() ──► 像素数据 ImageData
        │
        ▼
  ── 第二步：压缩循环（在内存里反复试，不出设备）──
        │
        第 1 轮  质量 0.85 编码 ──► 量体积
                    │
                    ├── ≤ 目标 ──────────────────► 成功，跳出
                    │
        第 2 轮  质量降到 0.70 再试 ──► 量体积
                    │
                    ├── ≤ 目标 ──────────────────► 成功，跳出
                    │
                   ……继续降……
                    │
        第 N 轮  质量已到下限 0.40
                    │
                    ├── ≤ 目标 ──────────────────► 成功，跳出
                    │
                    └── 仍超目标，改用第二手段：
                        长边缩到 70%，质量回到 0.85，重来一遍
                          │
                          ├── ≤ 目标 ────────────► 成功，跳出
                          │
                          └── 触到「质量下限 + 分辨率下限」
                              两个都到底了 ───────► 停止，如实报告
        │
        ▼
  ── 第三步：出结果 ──
  达标   ：Blob（压缩后的字节）+ 状态「已达标」
  未达标 ：同样给 Blob，但状态「未达标」
           并写清「实际 X，未达目标 Y」+ 一条可行动建议（M6）
        │
        ▼
  ── 第四步：出口（只有两个，都不出设备）──
  ① 页面显示：缩略图 +「原 X → 现 Y」+ 状态标识      （M4）
  ② 用户点下载：Blob 写进用户的下载文件夹             （M5）
        │
        ▼
  用户关掉页面 ──► 内存清空，什么都不留                （M7）
```

### 5.2 页面文件从哪来（部署链路）

照片不出设备，但**网页文件本身要送到用户眼前**，这一段是联网的：

```text
  你的电脑
     │  ① git push
     ▼
  GitHub 仓库（main 分支）
     │  ② GitHub Pages 自动发布
     ▼
  GitHub Pages 静态托管
     │  ③ 用户打开网址 → 下载 index.html / app.js / compress.js / style.css / *.wasm
     ▼
  用户的浏览器
     │  ④ 页面渲染完成
     ▼
  此后处理照片的过程 ──► 不再需要任何网络
```

**这张图的意义**：第 ③ 步是**双向网络里唯一的「下行」**——服务器只往外送代码，从不接收任何东西。用户的照片永远不会出现在这条链路的任何位置。

### 5.3 两个文件的分工

| 文件 | 管什么 | 输入 → 输出 |
|---|---|---|
| `app.js` | 界面、文件流、档位、调度、渲染 | 用户的拖放动作 → ImageJob 数组 → 屏幕上的列表 |
| `compress.js` | 压缩算法本身 | 一个 File + 目标字节数 → 一个 Blob + 用了什么参数 |

**这个分界要守住**：`compress.js` 里不应该出现任何界面代码。这样将来换压缩引擎（路线甲↔乙），只动这一个文件，界面完全不用碰。

---

## 6. 错误处理

原则：**能自己处理的就自己处理，处理不了的说人话，绝不假装成功。**

| # | 出错场景 | 用户看到什么 | 技术上怎么处理 |
|---|---|---|---|
| 1 | 拖进来的不是图片 | 「这 2 个文件不是图片，已跳过」 | 按 `mimeType` 过滤，标 `读取失败` |
| 2 | 图片损坏 / 解不开 | 「这张读不出来，可能文件损坏了」 | 捕获解码异常，标 `读取失败`，其他图继续 |
| 3 | **压缩到下限仍达不到目标** | 「实际 380KB，未达 200KB。这张照片细节太多，建议先裁掉无关部分再试」 | 走完循环 → 如实写 `未达标` + 建议文案（**M6 核心，PRD 第 5 节 C 类第 8 条：唯一不能吹的牛**） |
| 4 | **WASM 加载失败**（CDN 抽风 / 首次打开断网） | 用户无感 | **自动退回浏览器自带 Canvas 编码重试**，并在页面角落轻提示「已切换兼容模式」。见第 8 节迁移 2 |
| 5 | 张数太多 / 图太大，设备内存吃紧 | 「一次最多 20 张，先处理这 20 张吧」 | 限制 20 张（PRD 第 5 节 B 类第 5 条），逐张处理不并发全压 |
| 6 | 用户选了 HEIC（iPhone 默认格式） | 「暂不支持 iPhone 的 HEIC，请先在手机上转成 JPG」 | 检测到 `image/heic` 直接给明确提示，不静默失败 |
| 7 | 下载时文件名重复 | 无感 | 重名自动加序号，如 `照片(1).jpg` |
| 8 | 目标填了 0 或负数 | 输入框标红，下载按钮不可用 | 前端校验，不进入处理流程 |

**一条硬规矩**：场景 3 和场景 4 是两回事，不能混。**压不到是「结果」，加载失败是「故障」**。压不到必须如实告诉用户；加载失败必须自己兜住，不拿故障当结果。

---

## 7. 环境变量

**纯静态站点没有后端，也就没有服务器环境变量。** 对应的是代码顶部的一份**配置常量表**——改这些值就能调整行为，不用翻遍代码。

| 常量 | 建议默认值 | 作用 |
|---|---|---|
| `TARGET_PRESETS` | 见第 3.3 节档位表 | 档位按钮的取值 |
| `KB` | `1024` | 换算基数。**统一从这里取值**，避免各处写死 1024 或 1000 |
| `MAX_FILES` | `20` | 单次处理张数上限（PRD 第 5 节 B 类第 5 条） |
| `QUALITY_START` | `0.85` | 第一轮用的质量值 |
| `QUALITY_MIN` | `0.40` | 质量下限，到此不再降 |
| `SCALE_STEP` | `0.7` | 每次缩分辨率缩到原长边的 70% |
| `MIN_LONG_EDGE` | `800` | 分辨率下限（像素）。到此不再缩 |
| `USE_WASM` | `true` | **总开关。关掉即退回路线甲（浏览器自带编码器）** |
| `CDN_BASE` | `https://esm.sh` | 换 CDN 时只改这一处 |
| `SELF_HOST_ASSETS` | `false` | 改为本地托管 wasm 时置 `true`（见第 8 节迁移 1） |
| `SHOW_COMPAT_HINT` | `true` | 是否显示「已切换兼容模式」的轻提示 |

**注意**：`USE_WASM` 和 `SELF_HOST_ASSETS` 这两个开关是专门为「出事时能一键退」准备的。写代码时就要把它们做出来，不要等出事了再加——**那时候你正忙着救火，没空重构。**

---

## 8. 部署与迁移注意事项

### 8.1 部署（GitHub Pages）

| 步骤 | 做什么 | 完成后应看到 |
|---|---|---|
| 1 | 仓库 Settings → Pages → Source 选 `main` 分支、目录选 `/ (root)` | 页面提示「Your site is live at …」 |
| 2 | 等约 1 分钟 | 打开 `https://archmage15.github.io/vibe-coding/` 能看到页面 |
| 3 | 更新后重新推送 | 稍等片刻刷新页面即更新 |

**三个必须知道的点：**

1. **必须加 `.nojekyll` 空文件。** GitHub Pages 默认用 Jekyll 处理站点，会把下划线开头的文件和目录忽略掉。放一个空的 `.nojekyll` 文件在根目录，就能让它原样发布。**这不是可选项，少这个文件会导致某些资源 404。**
2. **所有 `.md` 文档也会被发布到网上。** 仓库本来就是公开的，这不增加暴露面，但要有这个意识。
3. **站点体积极小**（页面 + 代码 + wasm 大约 1MB 量级），远低于免费托管的常规限制。具体额度以 GitHub 官方文档为准。

### 8.2 迁移注意事项（四条，按最可能发生排序）

**迁移 1：CDN 在国内加载不出来 → 改成自托管**
症状：页面打不开或长时间白屏，F12 控制台显示来自 `esm.sh` 的请求失败。
做法：把 jSquash 的 JS 和 wasm 文件放进仓库（如 `vendor/` 目录），用 `<script type="importmap">` 把 `@jsquash/*` 映射到本地路径；`SELF_HOST_ASSETS` 置 `true`。
**关键：这条路仍然不需要构建工具。**

**迁移 2：WASM 在某些设备上加载失败 → 退回路线甲**
做法：`USE_WASM` 置 `false`，全站改用浏览器自带 Canvas 编码。
代价：压缩能力下降，极端档位（50KB）更容易达不到目标。届时 M6 的提示文案要相应调整。
**注意**：建议保留「WASM 失败自动退回」的运行时兜底，而不是只靠手动切开关——因为失败可能只发生在部分用户的设备上。

**迁移 3：将来要装 npm 包 → 引入 Vite 构建**
**会踩的坑（jSquash 官方文档已记录）**：Vite 的依赖预打包会破坏 wasm 加载，报 `Failed to construct 'URL': Invalid URL` 或 `Aborted(both async and sync fetching of the wasm failed)`。
解法：在 `vite.config.js` 里把相关包加进 `optimizeDeps.exclude`。
**提前知道这条，能省掉几个小时的无头排查。**

**迁移 4：换托管平台（Vercel / Cloudflare Pages）**
做法：纯静态文件，直接连仓库或拖文件夹即可，不需要任何服务端配置。
**注意**：这属于「换送货的机器」，与产品逻辑完全无关。但**不要**顺手上后端——一旦有后端，AC-M7 的隐私承诺就没了。

### 8.3 红线（不要做）

- ❌ 不要为了「统计多少人用了」加后端接口 —— 破坏隐私承诺（PRD 第 5 节 A 类）
- ❌ 不要引入账号系统 —— 同上
- ❌ 不要在压缩流程里发任何网络请求 —— 违反 AC-0
- ❌ 不要把用户的照片、文件名送到任何第三方 —— 文件名也可能含个人信息

---

## 9. 待核实与待验证

以下是**写入本文档但尚未亲手验证**的内容，不许当作事实引用（延续 PRD 第 6 节的做法）：

| # | 内容 | 来源 | 验证方式 |
|---|---|---|---|
| 1 | esm.sh / jsDelivr 在国内的实际可访问性与稳定性 | 无实测 | Day 6 写代码时直接打开页面看加载情况 |
| 2 | wasm 文件实际体积（MozJPEG 约 246KB、OxiPNG 约 160KB） | 第三方技术文章 | 打开 F12 网络面板看实际下载量 |
| 3 | MozJPEG 相对浏览器原生编码器「同画质小 10–15%」 | 第三方技术文章 | 拿同一张照片两条路线各压一次，比体积 |
| 4 | **纯前端能否把 5MB 照片压进 200KB / 50KB** | PRD 第 6 节第 1 条 | **Day 6 第一件事：拿真照片实测** |
| 5 | GitHub Pages 免费额度的具体数字 | 记忆，未核对 | 需要时查官方文档 |
| 6 | 中文报名系统是否普遍按 1024 换算 KB/MB | 推断 | 抽样看几个真实报名页面的提示文案 |
| 7 | HEIC 在目标用户里的实际占比 | 无数据 | 观察实际使用反馈 |

**第 4 条最要紧**：它决定路线乙是否真的够用，也决定第 6 节场景 3 的提示文案该怎么写。**建议 Day 6 的第一件事就是拿三张真实照片（含一张证件照）跑一遍。**

---

## 10. 附：本文档与 PRD 的对应关系

| 本文档章节 | 服务的 PRD 条目 |
|---|---|
| 1.3 选路线乙 | M3（自动压到目标以内）、M6（说人话的失败提示） |
| 2 项目结构 | 全局；AC-0（纯前端、无上传） |
| 3 数据模型 | M4（前后对比）、M6；AC-M7（关掉即清空） |
| 3.3 档位表 + 1024 换算 | M2（选目标档位） |
| 4.3 不存在的接口 | AC-0、AC-M7 |
| 5.1 数据流主图 | **AC-0、AC-M7（隐私承诺的技术依据）** |
| 5.2 部署链路 | AC-0（说明联网只发生在加载页面时） |
| 6 错误处理 | M6、AC-M3b（不假装成功） |
| 7 配置常量 | M2、M3；PRD 第 5 节 B 类第 5 条（20 张上限） |
| 8 部署与迁移 | AC-M7 的长期维持；PRD 第 6 节的待核实项 |

---

*本文档由 Day 5 工作产出，作为 Day 6 起编码的技术依据。*
